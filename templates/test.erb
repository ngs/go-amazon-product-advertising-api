package amazon

import "testing"

func Test<%= operation %>(t *testing.T) {
  client, _ := New("AK", "SK", "JP")
  req := client.<%= operation %>()
  if req == nil {
    t.Error("Expected not nil but got nil")
  }
  Test{client, req.Client}.Compare(t)
}
